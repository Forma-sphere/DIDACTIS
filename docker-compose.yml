services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: didactys
      POSTGRES_USER: didactys
      POSTGRES_PASSWORD: didactys_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
