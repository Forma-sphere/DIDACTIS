services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: didactis
      POSTGRES_USER: didactis
      POSTGRES_PASSWORD: didactis_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
